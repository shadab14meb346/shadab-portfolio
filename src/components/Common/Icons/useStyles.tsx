import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles((theme) => ({
  defaultArrow: {
    fontWeight: 400,
    fontSize: 36,
    "&:hover": {
      color: "#5221E6",
    },
  },
}));
